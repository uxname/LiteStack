# Матрица — LiteEnd-Go (backend) — 2026-06-20

> Невыполненная работа по матрице сбоев. Закрытые/принятые сценарии (MATRIX-PG, MATRIX-OIDC и все 🟢/🟡 «защита уже есть») удалены.

## Остаток (backlog, 🟠)

| ID | Цепочка / сценарий | В чём опасность | Решение | Файл |
|----|--------------------|-----------------|---------|------|
| MATRIX-DISK | Диск (uploads) переполнен | Запись падает (ENOSPC) → 500 «Failed to process file», частичный файл удаляется, метаданные не пишутся. Причина не видна в ответе и без алерта. | Проверять свободное место перед записью или хотя бы отдавать понятную ошибку + алерт по диску. Сейчас причина теряется в общем 500. | `internal/upload/service.go:120-124` |
| MATRIX-START | Старт: App → Postgres + Redis (SPOF на запуске) | На запуске нужны и БД, и Redis. Любой недоступен → `Build` падает → Docker рестартит по кругу → сайт не поднимается целиком, даже если жива половина зависимостей. | Сейчас осознанный fail-fast + `restart: unless-stopped`. Если нужна частичная доступность — разнести инициализацию HTTP/БД и Redis-функций (рефакторинг порядка инициализации). | `internal/app/app.go:48-83` |
| MATRIX-REDIS | Redis упал во время работы (деградация) | Подписки GraphQL и фоновая очередь перестают работать: `Subscribe`/`pump` повиснут или будут молча ничего не слать; `AddTestJob` вернёт ошибку enqueue, воркер не получает задач. Кэш и rate-limit уже фейлятся безопасно (fail-open). | При ошибке pub/sub закрывать подписку с понятной ошибкой клиенту, а не висеть тихо; ставить таймауты на pub/sub; обрабатывать ошибку enqueue на уровне резолвера и мониторить длину очереди. | `internal/profile/pubsub.go:44-67`, `internal/queue/queue.go:66-80` |
| MATRIX-BACKUP | Надёжность бэкапов | Бэкап один контейнер (`db_backup`), без мониторинга успешности. Ошибка лишь логируется, цикл едет дальше — узнаем только когда понадобится восстановление. Дампы лежат на том же хосте, что и БД (потеря хоста = потеря данных и бэкапов одновременно). Покрытие тестами 0%. | Мониторить успешность бэкапа (алерт, если нет свежего дампа > N часов) и вывозить дампы за пределы хоста (offsite, S3/другой хост). Тест восстановления. | `internal/backup/backup.go:42-58`, `docker-compose.yml` (volume `backups`) |
