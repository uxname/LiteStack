# Audit Report: Secrets Leak — 2026-06-20 21:47

> Невыполненная работа по аудиту секретов. Закрытые/принятые находки удалены.

## Остаток (backlog)

| Check ID | Проверка | Sev | Доказательство | Решение |
|----------|----------|-----|----------------|---------|
| SEC-06 | Комментарии в коде не содержат credentials | 🟡 | `Caddyfile:15` дефолт-фоллбэк в директиве содержит реальный bcrypt-хеш `{$ADMIN_PASSWORD_HASH:$2a$14$HAt9aLO...}`, а комментарии `Caddyfile:5` и `.env.example:42-43` явно документируют дефолт «admin / admin». Это раскрытый пароль admin-прокси по умолчанию. Риск: если оператор не переопределит `ADMIN_USER`/`ADMIN_PASSWORD_HASH`, все admin-дашборды (pgweb, RedisInsight, Asynqmon) и dev-страницы доступны под общеизвестными admin/admin. Смягчение: порты привязаны к `127.0.0.1` (`docker-compose.yml:161-163`). | **1. Убрать bcrypt-фоллбэк из `Caddyfile:15`** — сделать `ADMIN_PASSWORD_HASH` обязательным без дефолта, чтобы прокси не стартовал с дефолт-паролём \\ 2. Оставить фоллбэк только для dev и добавить runtime-проверку, запрещающую дефолтный хеш в production. Отложено: удаление dev-дефолта ломает локальный запуск без явной настройки; смягчено привязкой портов к `127.0.0.1`. Требует обязательной смены пароля при деплое. |
