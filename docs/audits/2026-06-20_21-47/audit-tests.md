# Audit Report: Test & Linter Integrity — 2026-06-20 21:47

Runtime: **Go** (модуль `github.com/uxname/liteend-go`). Чеклист изначально заточен под Node.js/TypeScript — пункты, специфичные для TS (strict mode, snapshot-тесты), адаптированы под Go: `tsconfig strict` → строгость `.golangci.yml` + `go test -race`; snapshot-политика к проекту неприменима.

Baseline: пуст (файл `docs/audit-baseline.yml` отсутствует) — записей `ACCEPTED` нет.

## Сводка

Тесты этого бэкенда — редкий случай высокого качества: реальные assertions во всех файлах, явные проверки failure-modes (спуфинг content-type, path traversal, невалидный payload, recovery из паники, инвалидация кэша), отсутствие `.only`/`.skip`/тавтологий. Линтер-конфиг очень строгий. Главные риски — организационные, а не «плохие тесты»: вся проверка качества держится **только на локальных git-хуках** (нет CI на сервере), и приложенный `coverage.out` отражает прогон **без интеграционных тестов**, поэтому сам по себе вводит в заблуждение.

| Check ID | Проверка | Статус | Уверенность | Доказательство | Решение | Исправлено |
|----------|----------|--------|-------------|----------------|---------|------------|
| TST-01 | Строгость типов/линтера (аналог strict mode) | ✅ PASS | High | `.golangci.yml:7-90` — включены `errcheck, govet, staticcheck, gosec, wrapcheck, nilerr, forcetypeassert, contextcheck` + комплексити-гейты `cyclop:15, funlen:80, gocognit:20`; `nolintlint.require-explanation:true` (запрет немотивированных `//nolint`); `forbidigo` запрещает `panic` и `fmt.Print*`. `Taskfile.yml:test` гоняет `go test -race`. Исключения для generated/тестов обоснованы и узкие | — | — |
| TST-02 | Пороги покрытия настроены и применяются | ❌ FAIL 🟠 | High | `.testcoverage.yml` задаёт пороги (total 38, resolver 90, queue 75, upload/profile/auth 60) и применяется через `task test:cov`. НО: (1) нет ни одной CI-конфигурации (`.github/workflows`, `.gitlab-ci.yml` отсутствуют) — гейт исполняется только в `pre-push` хуке (`lefthook.yml:13`), который обходится `git push --no-verify`; (2) приложенный `coverage.out` снят без `-tags=integration` (total 3.5%, resolver 11.5%, см. раздел «Покрытие») — он НЕ отражает реальное покрытие и не должен использоваться для оценки порогов | **1. Завести CI-пайплайн (GitHub Actions/GitLab CI), который гоняет `task test:cov` с Docker на каждый PR — сервер-сайд гейт, который нельзя обойти `--no-verify`** \\ 2. Оставить только хуки, но добавить branch protection / запрет force-push без прогона, и регенерировать `coverage.out` через `task test:cov` перед любым его использованием \\ 3. Документировать в AGENTS.md, что без Docker-прогона `coverage.out` невалиден, и не коммитить/не публиковать stale-профиль | Нет |
| TST-03 | Pre-commit/pre-push хуки запускают проверки | ✅ PASS | High | `lefthook.yml:7` — `pre-commit` → `task check` (codegen-freshness, fmt, tidy, build, lint, arch, deadcode, vuln, secrets); `lefthook.yml:13` — `pre-push` → `task test:cov` (unit+integration+пороги). `Taskfile.yml:check` подтверждает состав. Ограничение (обход `--no-verify` без сервер-сайд гейта) учтено в TST-02 | — | — |
| TST-04 | Критические пути покрыты тестами | ✅ PASS | High | auth: `internal/auth/middleware_test.go` (mock-user, импpersonation по sub, nil без bearer) + `context_test.go`; upload: `internal/upload/service_test.go:42-58` отклоняет disallowed mime и спуфленный content-type, `:75-84` блокирует path traversal; queue: `internal/queue/queue_test.go` — невалидный payload, recovery из паники→error для ретрая asynq; profile: `internal/profile/service_test.go` — create/cache/инвалидация кэша; resolver: `resolvers_test.go` (22 теста) + e2e `test/integration_test.go` (me, updateProfile+cache, upload+serve, отклонение non-image, WS-подписка, admin-only debug, i18n). Failure-modes покрыты явно | — | — |
| TST-05 | Тесты изолированы (нет shared mutable state) | ✅ PASS | High | Unit-тесты используют свежие fakes на каждый кейс (`newService()`, `newSvc(t)` с `t.TempDir()`), почти все помечены `t.Parallel()`. Fakes с мьютексами (`profile/service_test.go:18-21`). Integration делит один Postgres/Redis намеренно и поэтому НЕ параллелится — `paralleltest` для `^test/` отключён осознанно (`.golangci.yml` exclusions), `TestMain` корректно поднимает/гасит контейнеры | — | — |
| TST-06 | Нет пропущенных/зафиксированных тестов | ✅ PASS | High | `grep -rn "t.Skip\|SkipNow\|Skipf"` по `*_test.go` — 0 совпадений; `.only`-аналога в Go нет; закомментированных тестов не найдено | — | — |
| TST-07 | Тесты проверяют поведение, а не реализацию | ✅ PASS | High | Все 16 тест-файлов содержат assertions (`require.*`); тавтологий (`require.True(t, true)`) нет. Проверяется наблюдаемое поведение: HTTP-коды (`integration_test.go:148,173`), отклонение спуфинга (`upload/service_test.go:56`), ретрай при панике (`queue_test.go:43`), свежесть кэша после апдейта (`profile/service_test.go:162`). Плотность assertions здоровая (например upload 23 на 7 функций) | — | — |
| TST-08 | Нет нестабильных (flaky) тестов [⚡ dynamic] | ❌ FAIL 🟡 | Medium | `test/integration_test.go:205` — `time.Sleep(300 * time.Millisecond) // let the subscription register` перед триггером мутации. Фиксированный sleep вместо ожидания готовности подписки → на медленном/нагруженном CI подписка может не успеть зарегистрироваться, и событие будет пропущено (тест `TestSubscription_ProfileUpdated` иногда падает). Остальной файл корректно использует `require.Eventually` (`:189`). Math/Date-источников рандома в тестах нет | **1. Заменить `time.Sleep` на ожидание подтверждения регистрации подписки от сервера (например, дождаться ack/первого ожидаемого кадра в цикле `require.Eventually`)** \\ 2. Увеличить sleep и обернуть приём события в ретрай-цикл с большим дедлайном (паллиатив, не устраняет гонку) \\ 3. Оставить как есть и задокументировать как known-flaky с авто-ретраем теста в CI | Нет |
| TST-09 | Snapshot-политика | 🔍 UNVERIFIED | High | Snapshot-тестирование в проекте не применяется (Go, нет golden/snapshot-файлов в тест-директориях) — проверка неприменима | — | — |

## Дополнительные наблюдения (вне формального чеклиста)

- **`internal/backup` — 1725 statements, 0% покрытия, нет тест-файла.** Пакет НЕ исключён в `.testcoverage.yml` (там исключены только `cmd/`, generated, sqlc, model), поэтому его нули «съедают» общий `threshold.total` и удерживают его искусственно низким (38). Это не критический путь (auth/upload/queue/profile), но непокрытая логика бэкапа/восстановления БД — заметный риск надёжности. Рекомендация: добавить тесты на парсинг/формирование команд бэкапа либо явно исключить пакет в `.testcoverage.yml`, если он считается тонкой обёрткой над `pg_dump`.
- **Порог `total: 38` низкий, но честный** — он откалиброван от реального прогона (~39.9%) и в конфиге явно сказано «ratchet UP, never lower». Замечаний к методологии порогов нет; per-package floors (resolver 90 / queue 75 / upload·profile·auth 60) — корректная «TDD-защита» от логики без тестов в доменных пакетах.
- **`coverage.out` git-ignored** (`.gitignore:23`) — то есть в репозиторий не коммитится; приложенный файл локальный. Это правильно, но именно поэтому его значения нельзя трактовать как состояние гейта (см. TST-02).

## Audit Coverage
Проверено: `.golangci.yml`, `.testcoverage.yml`, `lefthook.yml`, `Taskfile.yml`, все `*_test.go` (16 файлов: auth, upload, queue, profile, resolver, graph, app, config, health, i18n, middleware, devtools, version, test/integration), `coverage.out` (агрегация по пакетам).
Пропущено: `internal/graph/generated/**`, `internal/db/sqlc/**`, `internal/graph/model/**` (generated), `db/migrations/**`, `assets/**`.
Файлов проверено: ~20 | Пропущено (generated/data): 3 каталога.
```
