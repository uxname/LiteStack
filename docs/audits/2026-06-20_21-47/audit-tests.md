# Audit Report: Test & Linter Integrity — 2026-06-20 21:47

> Невыполненная работа по аудиту тестов и линтеров. Закрытые/принятые находки удалены.

## Остаток (backlog)

| Check ID | Проверка | Sev | Доказательство | Решение |
|----------|----------|-----|----------------|---------|
| TST-02 | Пороги покрытия настроены и применяются | 🟠 | `.testcoverage.yml` задаёт пороги (total 38, resolver 90, queue 75, upload/profile/auth 60) и применяется через `task test:cov`. НО: (1) нет ни одной CI-конфигурации (`.github/workflows`, `.gitlab-ci.yml` отсутствуют) — гейт исполняется только в `pre-push` хуке (`lefthook.yml:13`), который обходится `git push --no-verify`; (2) приложенный `coverage.out` снят без `-tags=integration` (total 3.5%, resolver 11.5%) — он НЕ отражает реальное покрытие и не должен использоваться для оценки порогов. | **1. Завести CI-пайплайн (GitHub Actions/GitLab CI), который гоняет `task test:cov` с Docker на каждый PR — сервер-сайд гейт, который нельзя обойти `--no-verify`** \\ 2. Добавить branch protection / запрет force-push без прогона, регенерировать `coverage.out` через `task test:cov` перед любым его использованием. Отложено: CI-пайплайн — инфраструктурная задача (нужен Docker-раннер). |

## Требует ручной проверки

| Check ID | Проверка | Доказательство |
|----------|----------|----------------|
| TST-09 | Snapshot-политика | Snapshot-тестирование в проекте не применяется (Go, нет golden/snapshot-файлов в тест-директориях) — проверка неприменима. |
