# Audit Report: API Contracts — 2026-06-20 21:47

> Невыполненная работа по аудиту API-контрактов. Закрытые/принятые находки удалены.

## Остаток (backlog)

| Check ID | Проверка | Sev | Доказательство | Решение |
|----------|----------|-----|----------------|---------|
| API-03 | Error responses машиночитаемы и консистентны по структуре | 🟡 | REST-ошибки единообразны по форме `{statusCode, message}` (`internal/httperr/httperr.go:13-22`), но содержат **только числовой** `statusCode` + человекочитаемый `message` — нет строкового машиночитаемого кода. GraphQL же отдаёт строковый `code` (`UNAUTHENTICATED`/`FORBIDDEN`/`INTERNAL_SERVER_ERROR`), `statusCode` и `requestId` (`internal/graph/errors.go:27-40`). Клиент видит две разные модели ошибок: REST без `code` и без `requestId`, GraphQL — с ними. | **1. Добавить в `httperr.body` строковый `code` и `requestId` (взять из chi middleware.GetReqID), выровняв REST с GraphQL** \\ 2. Завести единый общий тип ошибки для обоих транспортов в отдельном пакете. Отложено: унификация модели ошибок REST/GraphQL — рефактор контракта. |
| API-07 | Публичный API имеет стратегию версионирования | 🟡 | Версионирования нет. REST-маршруты без префикса версии (`/upload`, `/health`, `/uploads/*` — `internal/app/app.go:135-139`); openapi.yaml несёт лишь `info.version: "0.0.1"` без URL/header-стратегии (`internal/devtools/openapi.yaml:8`). GraphQL-схема полагается на аддитивную эволюцию, но процедуры на случай переименования/смены типа поля не зафиксировано. API внутренний (фронт LiteStack — единственный клиент), поэтому риск умеренный. | **1. Зафиксировать политику эволюции в AGENTS.md: GraphQL — только аддитивные изменения + `@deprecated`, REST — версионный префикс при breaking-изменениях** \\ 2. Ввести версионный префикс REST (`/v1/...`) и `info.version` как контракт уже сейчас. Отложено: API внутренний, единственный клиент обновляется синхронно через мета-репозиторий. |
| API-01 | Форма ответов консистентна — единый envelope или его отсутствие | 🟢 | REST не использует единый envelope: успех `/health` → `{status, checks}` (`internal/health/health.go:43-46`), успех `/upload` → «голый» JSON-массив `[{filename, path}]` (`internal/upload/handler.go:91-98`), а ошибки → `{statusCode, message}` (`internal/httperr/httperr.go:13-16`). Три разных формы success-ответов; GraphQL отдельно соответствует своему конверту. | **1. Зафиксировать в openapi.yaml явное правило «без envelope» и описать схему ответа каждого endpoint** \\ 2. Ввести общий success-конверт (например `{data: ...}`) для всех REST-ответов. Отложено: введение единого success-конверта — изменение контракта, согласовать отдельно. |
