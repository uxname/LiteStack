# Audit Verification Report — 2026-06-20 21:43

Проект: `frontend` (LiteStack — React 19 + TanStack Start SSR, urql, zustand, FSD).

> Верификация исходного аудита завершена: false-positives — 0, устаревших артефактов — 0, пропущенных критических рисков — 0. Все подтверждённые находки FAIL закрыты или приняты. Ниже — только остаточная работа по верификации (проверки, которые невозможно подтвердить статически).

## Требует ручной проверки

- **ERR-04, ERR-06** — process-level handlers / graceful shutdown лежат в сгенерированном Nitro-бандле (`.output/server/index.mjs`, вне исходников). Корректно помечены 🔎 UNVERIFIED. **Проверить** поведение пресета `node-server` на SIGTERM / unhandled rejection в рантайме.
- **CON-05** — идемпотентность обмена OIDC `code→token` делегирована `react-oidc-context`/`oidc-client-ts` (внешний код). Подтверждается только динамически.
- **OWA-03** — resource ownership / IDOR: фактическая проверка — обязанность бэкенда, из фронт-репозитория не подтверждается.
- **LOG-04** — сквозной correlation-ID фронт→бэк не пробрасывается; требует анализа всей цепочки запросов в рантайме.
