# Audit Report: Test & Linter Integrity — 2026-06-20 21:43

Проект: `frontend` (litefront). Стек тестов: Vitest 4 + @testing-library/react, Playwright (E2E), Ladle (stories). Линтеры: Biome, Stylelint, Steiger (FSD), Knip.

## Осталось сделать (backlog)

| Check ID | Проверка | Sev | Доказательство | Решение (остаток) |
|----------|----------|-----|----------------|-------------------|
| TST-04 | Критические пути покрыты тестами (error handling профиля) | 🟡 | Error-handling двух критических путей профиля не покрыт юнит-тестом: (1) `src/features/profile/api/upload-avatar.ts:29-37` — ветки `throw` при `!response.ok` и при отсутствии `path` в ответе не имеют ни одного теста (сам модуль `uploadAvatar` не тестируется, в `ProfileForm.test.tsx:13` только мок-заглушка); (2) `src/features/profile/lib/useProfileForm.ts:55-59,77-80` — ветки `toast.error` при сбое загрузки аватара и при `result.error` мутации не покрыты. Риск: регресс в обработке ошибок загрузки/сохранения профиля пройдёт незамеченным. | **1. Добавить юнит-тест `upload-avatar.test.ts` с моком `fetch`: успех, `!response.ok` (throw), пустой/без-path ответ (throw).** \\ 2. Расширить `ProfileForm.test.tsx` сценариями submit с ошибкой мутации и неудачной загрузки аватара (проверять вызов `toast.error`) через `@testing-library/user-event`. \\ 3. Покрыть оба пути на уровне E2E в `tests/e2e/pages/account.spec.ts` с перехватом `page.route('**/upload')`. |
| TST-02 | Coverage thresholds применяются в CI | 🟡 | Пороги заданы и адекватны (`vitest.config.ts:26-31` — lines 82 / functions 85 / branches 78 / statements 82), но настоящего CI нет: `.github/workflows` и `.gitlab-ci.yml` отсутствуют. Покрытие гоняется только в локальном pre-push хуке (`package.json:43` → `test:cov` внутри `verify:push`), который обходится флагом `git push --no-verify`. Серверного гейта, который нельзя обойти, нет. | Добавить CI-workflow (GitHub Actions / GitLab CI), запускающий `npm run verify:push` на каждый PR/push — серверный гейт, не обходимый `--no-verify`. (Инфраструктурная задача.) |
