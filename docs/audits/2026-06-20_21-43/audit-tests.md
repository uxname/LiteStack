# Audit Report: Test & Linter Integrity — 2026-06-20 21:43

Проект: `frontend` (litefront). Стек тестов: Vitest 4 + @testing-library/react, Playwright (E2E под `tests/e2e`), Ladle (stories). Линтеры: Biome, Stylelint, Steiger (FSD), Knip. TypeScript 6. Хуки: Lefthook.

Baseline: `docs/audit-baseline.yml` — пустой (только шаблон), принятых исключений нет.

| Check ID | Проверка | Статус | Уверенность | Доказательство | Решение | Исправлено |
|----------|----------|--------|-------------|----------------|---------|------------|
| TST-01 | TypeScript strict mode включён | ✅ PASS | High | `tsconfig.json:25` — `"strict": true`; дополнительно `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch` (строки 26-28). В исходниках и тестах нет `@ts-ignore`/`@ts-expect-error` (поиск по `src`/`tests` — 0 совпадений). Biome дополнительно ловит неиспользуемый код как `error` (`biome.json:33-36`). Замечание: `useUnknownInCatchVariables: false` (`tsconfig.json:4`) делает `catch (e)` типом `any` — ослабление, но не отключение strict. | — | — |
| TST-02 | Coverage thresholds настроены и применяются в CI | ❌ FAIL 🟡 | High | Пороги заданы и адекватны: `vitest.config.ts:26-31` — lines 82 / functions 85 / branches 78 / statements 82. Но настоящего CI нет: `.github/workflows` и `.gitlab-ci.yml` отсутствуют (поиск — 0 файлов). Покрытие гоняется только в локальном pre-push хуке (`package.json:43` → `test:cov` внутри `verify:push`), который обходится флагом `git push --no-verify`. То есть порог не обязателен на стороне сервера/CI — единственный гейт чисто локальный и отключаемый. | **1. Добавить CI-workflow (GitHub Actions / GitLab CI), запускающий `npm run verify:push` на каждый PR/push — серверный гейт, который нельзя обойти `--no-verify`** \\ 2. Оставить только хуки, но запретить `--no-verify` политикой ревью и задокументировать это в `frontend/AGENTS.md` \\ 3. Принять как осознанный риск для шаблонного репозитория и зафиксировать в `docs/audit-baseline.yml` | Нет |
| TST-03 | Pre-commit/pre-push хуки запускают проверки (tests, lint, typecheck) | ✅ PASS | High | `lefthook.yml:7-15` — pre-commit → `npm run verify:commit` (`package.json:42` = `check` + `secrets`; `check` включает `ts:check`/`tsc --noEmit`, `lint:fix` (Biome), `lint:style`, `knip`, `lint:fsd`). pre-push → `verify:push` добавляет `test:cov`, `test:e2e:prod`, `storybook:build` (`package.json:43`). Установка хука гарантируется `prepare`/`postinstall` (`package.json:45-46`). | — | — |
| TST-04 | Критические пути покрыты тестами (auth, validation, error handling) | ❌ FAIL 🟡 | High | Покрыто хорошо: auth `buildAccountCenterUrl` (`tests/unit/features/auth/account-center.test.ts`), AuthObserver включая ветку ошибки OIDC и silent-renew (`src/app/providers/AuthObserver.test.tsx:60-87`), GraphQL-клиент с/без токена (`src/shared/api/graphql-client.test.ts`), валидация профиля по границам (`src/features/profile/model/schema.test.ts`). НО error-handling двух критических путей профиля не покрыт юнит-тестом: (1) `src/features/profile/api/upload-avatar.ts:29-37` — ветки `throw` при `!response.ok` и при отсутствии `path` в ответе не имеют ни одного теста (поиск `uploadAvatar` в `*.test.*` — только мок-заглушка в `ProfileForm.test.tsx:13`, сам модуль не тестируется); (2) `src/features/profile/lib/useProfileForm.ts:55-59,77-80` — ветки `toast.error` при сбое загрузки аватара и при `result.error` мутации не покрыты (ProfileForm-тест проверяет только рендер, не submit/upload-ошибки). Риск: регресс в обработке ошибок загрузки/сохранения профиля пройдёт незамеченным. | **1. Добавить юнит-тест `upload-avatar.test.ts` с моком `fetch`: успех, `!response.ok` (throw), пустой/без-path ответ (throw)** \\ 2. Расширить `ProfileForm.test.tsx` сценариями submit с ошибкой мутации и неудачной загрузки аватара (проверять вызов `toast.error`) через `@testing-library/user-event` \\ 3. Покрыть оба пути на уровне E2E в `tests/e2e/pages/account.spec.ts` с перехватом `page.route('**/upload')` | Нет |
| TST-05 | Тесты изолированы — нет shared mutable state между тестами | ❌ FAIL 🟡 | Medium | `tests/setup.ts:37-58` объявляет глобальные `vi.mock` (paraglide messages, `react-oidc-context`), а `vitest.config.ts` НЕ задаёт `restoreMocks`/`mockReset`/`clearMocks` (поиск в конфиге — 0). Изоляция держится на том, что каждый suite вручную зовёт `vi.clearAllMocks()` в `beforeEach`/`afterEach` (например `AuthObserver.test.tsx:25-28`, `graphql-client.test.ts:26`). Если новый тест забудет этот вызов, состояние глобального мока `useAuth` (переопределённого через `mockReturnValue`) протечёт в следующие тесты внутри файла. Это латентный риск изоляции, а не активный баг — фактических протечек в текущем коде не найдено. Доп. наблюдение (поддерживаемость, не изоляция): два параллельных набора тестов — co-located `src/**/*.test.*` (22 файла) и `tests/unit`+`tests/component` (8 файлов) — частично дублируют друг друга (ProfileForm, counter store, graphql-client, profile schema тестируются в обоих местах с разным числом ассертов), что удваивает стоимость поддержки. | **1. Включить `test.restoreMocks: true` (или `clearMocks: true`) в `vitest.config.ts`, чтобы изоляция не зависела от ручных `clearAllMocks` в каждом suite** \\ 2. Оставить ручную очистку, но добавить lint-правило/ревью-чеклист, требующий `beforeEach(() => vi.clearAllMocks())` в каждом файле с моками \\ 3. Консолидировать дублирующиеся наборы (`tests/unit`+`tests/component` против co-located) в один и задокументировать соглашение о расположении тестов | Нет |
| TST-06 | Нет пропущенных или зафиксированных тестов (.only/.skip без обоснования) | ✅ PASS | High | Поиск `.only(`/`.skip(`/`xit`/`fdescribe`/`fit` по `src` и `tests` — 0 совпадений. Playwright дополнительно валит сборку на оставленный `test.only` в CI: `playwright.config.ts:18` — `forbidOnly: !!process.env.CI`. Закомментированных тест-блоков не обнаружено. | — | — |
| TST-07 | Тесты проверяют поведение, а не детали реализации | ✅ PASS | High | Тесты ассертят наблюдаемое поведение, а не внутренности: ProfileForm — через роли/лейблы (`getByRole`, `getByLabelText`, `toBeDisabled` — `ProfileForm.test.tsx:30-66`); схема — через `safeParse` результат и пути ошибок (`schema.test.ts`); GraphQL-клиент — через конфиг, переданный в конструктор (`graphql-client.test.ts`). Tautology-тестов (`expect(true).toBe(true)`), пустых `expect` не найдено. Единственное — `tests/sum.test.ts` — тривиальный демо-тест локальной функции `sum`, не привязан к коду приложения (низкая ценность, но не нарушение). | — | — |
| TST-08 | Нет нестабильных тестов (Math.random, Date.now без mock, sleep) [⚡ dynamic] | 🔍 UNVERIFIED | Medium | Статический поиск `Math.random`/`Date.now`/`new Date(`/`setTimeout`/`sleep(` по тест-файлам: единственное совпадение — `src/pages/account/lib/account.test.ts:133` `new Date(String(iso)).toLocaleDateString()`, где входной `iso` фиксирован, а не текущее время — детерминированно. Тестовых таймеров (`vi.useFakeTimers`) нет, но и зависимостей от реального времени/рандома в тестах не обнаружено. Проверка помечена `[⚡ dynamic]` — статически PASS поставить нельзя; явных нарушений нет. | — | — |
| TST-09 | Snapshot-тесты охватывают значимые изменения, не весь DOM/объект целиком | ✅ PASS | High | Поиск `toMatchSnapshot`/`toMatchInlineSnapshot` по `src` и `tests` — 0 совпадений. Snapshot-тесты не используются; компонентные тесты делают точечные ассерты по ролям/атрибутам. Риск «хрупкого снапшота на весь DOM» отсутствует. | — | — |

## Сводка

- 🔴 Critical: 0
- 🟠 High: 0
- 🟡 Medium (FAIL): 3 — TST-02 (нет серверного CI-гейта для coverage), TST-04 (error-handling профиля не покрыт юнитами), TST-05 (изоляция моков держится на ручной очистке + дублирование наборов тестов)
- 🟢 Low: 0
- 🔍 UNVERIFIED: 1 — TST-08 (dynamic)
- ✅ PASS: 5 — TST-01, TST-03, TST-06, TST-07, TST-09

Блокирующих (🔴/🟠) находок нет. Все три FAIL — 🟡 Medium, решение по ним необязательно, но рекомендуется. Самый значимый по сути — TST-04 (тестовые пробелы в обработке ошибок критического пути «профиль»).

## Audit Coverage
Проверено: tsconfig.json, vitest.config.ts, vite.config.ts, lefthook.yml, biome.json, playwright.config.ts, package.json, tests/setup.ts, src/features/auth/**, src/features/profile/**, src/shared/api/**, src/app/providers/AuthObserver*, все `src/**/*.test.*` (22) и `tests/unit`+`tests/component` (8), `tests/e2e/*` (перечень).
Пропущено: tests/e2e/** (содержимое E2E-спеков читалось только по списку), src/generated/**, src/widgets/** и прочие не-критические UI-компоненты (тела тестов выборочно), scripts/**, stories (`*.stories.*`).
Файлов проверено: ~18 ключевых | Пропущено: ~остальные тела не-критических тестов и генерируемый код.
