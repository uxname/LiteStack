# Meta-repo only — sub-projects manage their own ignores.
.DS_Store
*.log
.idea/
.vscode/

# Meta-repo tooling deps (CodeGraph CLI + scripts)
/node_modules/

# Cursor MCP config holds a machine-absolute project path (codegraph --path); regenerate via `npm run codegraph:install`
/.cursor/mcp.json
